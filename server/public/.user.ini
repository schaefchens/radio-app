; Read by PHP-FPM for every script under the web root. php_value in .htaccess
; is a 500 under FPM, so limits live here. The host may cap these; the probe
; reports what actually applies.
upload_max_filesize = 8M
post_max_size = 10M
max_execution_time = 60
memory_limit = 256M
expose_php = Off
