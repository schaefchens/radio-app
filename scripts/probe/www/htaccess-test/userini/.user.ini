; PHP-FPM reads .user.ini per directory (cached user_ini.cache_ttl seconds,
; default 300 — a freshly uploaded one can take five minutes to apply).
; These are the values the audio upload endpoint needs.
upload_max_filesize = 8M
post_max_size = 10M
